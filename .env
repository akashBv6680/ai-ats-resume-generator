# === Global ===
NODE_ENV=development

# === Backend ===
BACKEND_PORT=5000                # because your compose exposes backend on 5000
MONGO_URI=mongodb://admin:admin123@mongodb:27017/ats-resume-db?authSource=admin
JWT_SECRET=some-very-long-random-string-here
JWT_EXPIRES_IN=1h

# URL for internal AI service (Python FastAPI)
AI_SERVICE_URL=http://ai-service:8000

# === AI Service ===
AI_SERVICE_HOST=0.0.0.0
AI_SERVICE_PORT=8000

# Local LLM (Ollama) – must be self-hosted, no external AI APIs
# On your laptop: install and run `ollama serve` + `ollama run mistral`
AI_SERVICE_OLLAMA_BASE_URL=http://host.docker.internal:11434

# === Frontend ===
FRONTEND_PORT=3000

# === Misc / CI ===
DOCKER_REGISTRY=your-docker-registry
