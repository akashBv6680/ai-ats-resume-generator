# Jenkins CI/CD

This folder is reserved for Jenkins pipeline definitions.

Example files you can add:
- `Jenkinsfile` for multibranch pipelines
- `deploy.groovy` for environment-specific deployment logic

GitHub Actions is the default CI/CD in this project; Jenkins is optional.
