# Grafana Setup

This directory is reserved for Grafana dashboards and provisioning files.

You can store:
- JSON dashboard exports for:
  - Request latency
  - Error rates
  - ATS score distribution
  - AI service latency

Connect Grafana to Prometheus as the data source.
