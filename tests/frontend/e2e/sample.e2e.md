# Frontend E2E Scenarios (Placeholder)

Use your preferred tool (Playwright/Cypress) to implement:

1. User login and dashboard load.
2. Create resume -> live ATS score appears -> resume saved in history.
3. Admin login -> template list visible.
